# This crate is a copy from Facebook Sapling SCM
# https://github.com/facebook/sapling/tree/main/eden/scm/lib/minibytes (rev f98f3e94e6687e831dcc27bf52fabd5eb96f5bba)
# Technically we can pull this crate via git dependency, but cloning underlyning repo takes a *very* long time

# @generated by autocargo

[package]
name = "minibytes"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
bytes = { version = "1.1", features = ["serde"], optional = true }
memmap2 = { version = "0.7.0", optional = true }
serde = { version = "1.0.136", features = ["derive", "rc"] }

[dev-dependencies]
quickcheck = "1.0"

[features]
default = ["frombytes", "frommmap"]
frombytes = ["bytes"]
frommmap = ["memmap2"]
